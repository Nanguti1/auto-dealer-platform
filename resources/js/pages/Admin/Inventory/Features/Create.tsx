import InventoryShell, { BackButton } from '@/components/admin/inventory/inventory-shell'; import { FeatureForm } from '@/components/admin/inventory/simple-resource-form'; import { admin } from '@/routes/admin'; export default function Create() { return <InventoryShell title="Create Vehicle Feature" actions={<BackButton href={admin.vehicleFeatures.index().url} />}><FeatureForm action={admin.vehicleFeatures.store.form().action} /></InventoryShell>; }
