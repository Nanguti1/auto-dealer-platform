import InventoryShell, { BackButton } from '@/components/admin/inventory/inventory-shell'; import { FeatureForm } from '@/components/admin/inventory/simple-resource-form'; import type { AdminFeature } from '@/components/admin/inventory/types'; export default function Edit({ vehicleFeature }: { vehicleFeature: AdminFeature }) { return <InventoryShell title="Edit Vehicle Feature" actions={<BackButton href={`/admin/vehicle-features/${vehicleFeature.id}`} />}><FeatureForm feature={vehicleFeature} action={`/admin/vehicle-features/${vehicleFeature.id}`} /></InventoryShell>; }
